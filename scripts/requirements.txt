# Classicap Dataset Requirements
# ===============================

# REQUIRED: YouTube downloader
# CRITICAL: Must be 2025.10.22 or later
# Earlier versions (2024.x and below) have signature extraction issues
# that will cause HTTP 403 errors
yt-dlp>=2025.10.22

# OPTIONAL: Audio verification (for verify_dataset.py)
# These are only needed if you want to verify audio format/quality
# The download script works without these
librosa>=0.10.0
soundfile>=0.12.1  # Required by verify_dataset.py for audio format checking
scipy>=1.11.0
numpy>=1.24.0

# OPTIONAL: Progress bars (recommended)
tqdm>=4.66.0

# OPTIONAL: Statistics and visualization (for dataset analysis)
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
